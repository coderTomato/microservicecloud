spring:
  cloud:
    config:
      #指定configserver的地址
      uri: http://localhost:9999
      #指定要访问的远程库分支
      label: master
      #指定要从远程库读取的配置文件名称， 无需扩展名
      name: application-consumer-config
      #环境选择
      profile: dev